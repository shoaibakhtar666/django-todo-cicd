pipeline {
  agent any
  stages {
    stage('Build') {
     agent {
        docker { image 'django:3.2' }
      }
      steps {
        sh 'python3 manage.py runserver 0.0.0.0:8000'
      }
    }
}
}
